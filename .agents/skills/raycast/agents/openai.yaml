interface:
  display_name: "Raycast"
  short_description: "Repo-specific patterns and workflows for raycast"
  default_prompt: "Use the raycast repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true