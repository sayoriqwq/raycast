{
  "name": "counter",
  "title": "Counter",
  "description": "Generic event counter with daily summary and audit logs",
  "icon": "assets/icon.png",
  "author": "sayori",
  "license": "MIT",
  "commands": [
    {
      "name": "increment-counter",
      "title": "Increment Counter",
      "description": "Increment a named counter and write daily summary + audit log",
      "mode": "no-view",
      "arguments": [
        {
          "name": "name",
          "placeholder": "Counter name, e.g. qq",
          "type": "text",
          "required": true
        }
      ]
    },
    {
      "name": "increment-qq",
      "title": "Increment QQ Counter",
      "description": "Increment the qq counter and write daily summary + audit log",
      "mode": "no-view"
    }
  ],
  "dependencies": {
    "@raycast/api": "latest"
  },
  "devDependencies": {
    "@types/node": "latest",
    "typescript": "latest"
  },
  "scripts": {
    "dev": "ray develop",
    "build": "ray build -e dist",
    "lint": "ray lint"
  }
}